// Seeding is disabled for Supabase.
// Users will create their own data through the app after authenticating.
// To seed data programmatically, you would use the server admin client
// and create data associated with specific user UUIDs from auth.users table.

export async function seedDatabase() {
  // No seeding for Supabase backend
}
